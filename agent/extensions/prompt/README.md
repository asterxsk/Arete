# prompt

Injects custom system instructions into the system memory before every agent turn.

## Features

- Appends a `## Custom Instructions` block to the system prompt.
- Encourages using PowerShell by default.
- Promotes the use of todos to track multiple steps.
- Promotes the use of background tasks and scheduling.

## Removal

Delete the `prompt/` folder to remove these custom instructions from your environment.
